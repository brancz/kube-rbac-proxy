apiVersion: v1
kind: Service
metadata:
  labels:
    app: kube-rbac-proxy
  name: kube-rbac-proxy
  namespace: default
spec:
  ports:
    - name: https
      port: 8443
      targetPort: https
    - name: proxy
      port: 8643
      targetPort: proxy
  selector:
    app: kube-rbac-proxy
