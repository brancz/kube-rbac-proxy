apiVersion: v1
kind: ConfigMap
metadata:
  name: kube-rbac-proxy
data:
  config-file.yaml: |+
    authorization:
      static:
        - user:
            name: system:serviceaccount:default:client-with-static-match
          resourceRequest: false
          verb: get
          path: /metrics
