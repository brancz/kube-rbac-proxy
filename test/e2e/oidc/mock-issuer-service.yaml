apiVersion: v1
kind: Service
metadata:
  labels:
    app: mock-issuer
  name: mock-issuer
spec:
  ports:
    - name: api
      port: 8443
      protocol: TCP
      targetPort: api
  selector:
    app: mock-issuer
  type: ClusterIP
